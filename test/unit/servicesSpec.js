'use strict';

describe("Drawing Service", function() {
    var $scope, service;
/*
    beforeEach(module('csTest'));

    beforeEach(function () {
        inject(function($controller, _$rootScope_, _$q_, drawingService){
            debugger;
            service = drawingService;
            $scope = _$rootScope_.$new();
        })
    });

    it('matching passed color', function() {
        debugger
        //var colors = service.getColorArray();
        //expect('#1ab394').toEqual(colors.passedColor);
    });
    */
});